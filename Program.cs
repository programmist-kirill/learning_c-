using System.Security.Cryptography.X509Certificates;

void calculating() {
    string valut_user;
    
    Console.Write("какой валюты у вас деньги -");

    valut_user = Console.ReadLine();
    if (valut_user == "rus");
    {
        string count_cashe_user;
        
        Console.Write("Сколько у вас денег в этой валюте")
    }
}

calculating();